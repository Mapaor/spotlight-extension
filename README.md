# Spotlight Focus
A Firefox extension to screenshot a part of a page to spotlight it by making the rest of the page white/blur.

Also works with PDFs using a custom extension page to load them inside an iframe.

The spotlighted area is also draggable to better position it.